# /public/resume

Drop your resume here with this exact filename:

`Aboobaker-Siddique-Resume.pdf`

The "VIEW RESUME" and "DOWNLOAD CV" buttons on the site already point to
`/resume/Aboobaker-Siddique-Resume.pdf`. Once the file exists at this path,
both buttons work immediately — no code changes needed.

If the file is missing, the buttons still render (no broken links); the
person who lands here will just get a 404 if they click, so add the file
before publishing the site.

See `CUSTOMIZATION.md` at the project root for full instructions.
